services:
  app:
    build:
      target: dev
    image: radio-calipso:dev
    environment:
      - NODE_ENV=development
    volumes:
      - ./:/app
      - /app/node_modules
      - sqlite_data:/app/data
    restart: "no"
